name: Timing Audit
on:
  push:
    branches: [sync-match-timings-v1]
jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Probe timing reconstruction
        run: node scripts/timing-audit.js
