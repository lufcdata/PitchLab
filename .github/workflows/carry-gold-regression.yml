name: Carry Gold Regression
on:
  push:
    branches: [carry-brighton-v6-audit]
jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Forest carry control
        run: node scripts/carry-gold-regression.js
