# PitchLab Free-Kick Family Forensic Audit — 2026-08-28

## Status

GOLD_LOCKED — forensic validation complete on two independent raw fixtures.

## Authoritative definition

`Free-Kick Pass = Pass + FreekickTaken, excluding IndirectFreekickTaken`

The implementation also retains the standard restart-family exclusions for corners, throw-ins, goal kicks and penalties.

This is an event-level qualifier rule. Never replace it with `raw free kicks - offsides`, a timing heuristic, event IDs, fixture IDs, or any fixture-specific correction.

## Primary control — Nottingham Forest 0–1 Leeds

WhoScored match ID: `1983552`.

Raw `Pass + FreekickTaken` population: **16–16**.

Exactly **3 Forest + 3 Leeds** raw restart passes also carry `IndirectFreekickTaken`. Event-level inspection confirms all six are the restart passes generated by the fixture's six offsides. Excluding those explicitly qualified indirect restarts produces the displayed Free-Kick Pass population exactly:

- Free-Kick Passes: Forest **13**, Leeds **13**.
- Accurate Free-Kicks: Forest **8**, Leeds **6**.
- Free-Kicks In the Final Third: Forest **2**, Leeds **1**.

The canonical shot family remains unchanged:

- Shots From Free-Kicks (`shotFamily && DirectFreekick`): Forest **1**, Leeds **2**.

Therefore the headline decomposition is:

- Forest: `13 Free-Kick Passes + 1 Direct Free-Kick Shot = 14 Total Free Kicks`.
- Leeds: `13 Free-Kick Passes + 2 Direct Free-Kick Shots = 15 Total Free Kicks`.

## Independent second fixture — Bournemouth 2–2 Leeds

WhoScored match ID: `1903384`.

The identical qualifier rule survives without adjustment:

- Raw `Pass + FreekickTaken`: Bournemouth **11**, Leeds **7**.
- `IndirectFreekickTaken` exclusions: Bournemouth **1**, Leeds **1**.
- Canonical Free-Kick Passes: Bournemouth **10**, Leeds **6**.
- Accurate Free-Kicks: Bournemouth **6**, Leeds **1**.
- Free-Kicks In the Final Third: Bournemouth **1**, Leeds **1**.

Event-level inspection confirms both excluded passes are offside-generated restart passes and explicitly carry both `FreekickTaken` and `IndirectFreekickTaken`.

## Why this closes the investigation

The original 16–16 Forest mismatch was not solved by arithmetic subtraction. The raw event stream exposes a dedicated qualifier that identifies the unwanted population directly. The same relationship independently appears in Bournemouth–Leeds, where one offside restart per team carries `IndirectFreekickTaken` and is excluded by the same predicate.

The rule therefore satisfies PitchLab's forensic requirements:

1. Forest–Leeds reproduces **13–13** from event-level qualifiers.
2. Existing Gold Direct Free-Kick shots remain **1–2**.
3. Forest headline total reconstructs **14–15**.
4. Bournemouth–Leeds independently survives the identical predicate.
5. The canonical registry entry is exposed to Pitch Events, Metric Leaders and Match Stats.

## Canonical implementation

Owned by `ui-free-kick-definition.js`, version `FREE_KICK_GOLD_V3_2026-08-28`.

The three Gold registry metrics are:

- `free_kicks`
- `free_kicks_accurate`
- `free_kicks_final_third`

All are `GOLD_LOCKED` and expose the canonical predicate through `window.PitchLabMetricBible.canonicalRegistry`.

## Guardrails

- Never implement `raw free kicks - offsides`.
- Never use fixture IDs or event IDs in the predicate.
- Never infer the restart source from location alone.
- Never use chronology when `IndirectFreekickTaken` is explicitly available.
- Do not change the Gold Direct Free-Kick shot definition as part of this family.
- Do not allow a legacy `FreeKickTaken`/`free_kick` alias to override `FreekickTaken` plus the canonical exclusion.
- Any future provider/schema change must be revalidated before this Gold definition is altered.

## Closure

FREE-KICK FAMILY CLOSED / GOLD LOCKED — 28 August 2026.
