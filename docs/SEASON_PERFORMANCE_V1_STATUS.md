# Season Performance V1 status

Branch: `season-performance-v1`

Current state: functional first implementation on an isolated branch.

The page shell, season controls, multi-file JSON import, date filtering, player filtering, metric filtering, match-period controls, match counter, match-safe carry aggregation, Event Map/Heat Map reuse and 1080 x 1350 PNG export are implemented.

The production manifest is present. The hosted compressed fixture packs are the remaining ingestion task before the page can auto-load the three current season fixtures without using the local multi-file import fallback.
